Off season rewrite of 2026 code. Goals for the project are as follows:

* Try AdvantageKit and evaluate if we want to run it in 2027
* Try splitting the code into more subsystems. We've run into pain points in the past when combining actuators of not being able to create granular commands easily.
* Import CAD files for 3D simulation.
* Use photonvision instead of limelight software. (probably still running on limelight hardware at first)
* Attempt physics based targeting instead of lookup tables.
